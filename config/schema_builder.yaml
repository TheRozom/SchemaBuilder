schema_building:
  auto_detect_groups: true

  similarity_threshold_for_anyOf: 50

  strategy:
    single_group: "merge"
    multiple_groups: "anyOf"

  grouping:
    min_similarity_for_same_group: 80
    use_analyzer_groups: true
